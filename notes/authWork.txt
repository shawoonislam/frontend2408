step-2
Good. Since these are UI-only for now (no live API calls — matching what you said earlier about wiring backend logic yourself later), I'll build the forms with local state, validation, and console.log-style submit handlers you can swap for real API calls in Step 2's follow-up.
Design approach for auth pages: a split-screen layout — brand panel on the left (ink background, subtle pattern, reinforces identity), form on the right. Consistent across all 6 auth pages via one shared layout component, so switching between Login/Register/etc. feels like one continuous flow, not disconnected pages.
